'use client';
import { useState } from 'react';
import Link from 'next/link';
import { useForge } from './ForgeProvider';
export function ReviewDrawer() { const { review, closeReview, confirm, reject } = useForge(); const [reason, setReason] = useState(''); if (!review) return null; return <aside className="reviewDrawer" aria-label="Edit review drawer"><div className="drawerHeader"><div><span className="eyebrow">HUMAN GATE</span><h2>Review proposed edit</h2></div><Link href="/proof" className="quietButton">Inspect proof</Link><button className="iconButton" onClick={closeReview} aria-label="Close review">×</button></div><div className="reviewBody"><div className="pendingBadge">Pending review</div><pre>{review.proposedBody}</pre><div className="provenance"><span className="eyebrow">PROVENANCE</span>{Object.entries(review.provenance).map(([source, url]) => <a key={source} href={url} target="_blank" rel="noreferrer">{source} ↗</a>)}</div><label>Rejection reason (optional)<input value={reason} onChange={(event) => setReason(event.target.value)} /></label></div><div className="drawerActions"><button className="quietButton" onClick={() => void reject(review.id, reason)}>Reject edit</button><button className="primaryButton" onClick={() => void confirm(review.id)}>Confirm edit</button></div></aside>; }
